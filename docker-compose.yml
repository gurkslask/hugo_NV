version: "2"

services:
 server:
  image: klakegg/hugo:0.101.0-ext-ubuntu
  command: server -D
  networks:
    - main
  volumes:
    - "./nya_varlden:/src"
  ports:
    - "81:1313" 
      
networks:
  main: